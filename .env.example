# LASTRO — variáveis de ambiente
# Copie para .env e preencha localmente; nunca commitar valores reais.

NODE_ENV=development
PORT=3000

# Variáveis das fases futuras do plan.md (F24 em diante: DATABASE_URL;
# F26–F29: segredos Kiwify/Resend) entram aqui quando essas fases começarem —
# não antecipar valores sem fonte confirmada (CLAUDE.md regra 6).
